# from pymongo import MongoClient

# # Connect to local MongoDB
# client = MongoClient("mongodb://localhost:27017/")
# db = client["chess_gpt_db"]

# # Collections
# users_col = db["users"]
# games_col = db["games"]
