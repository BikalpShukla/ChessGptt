# from gpt4all import GPT4All

# # Initialize GPT4All model (download required model first)
# model = GPT4All("ggml-gpt4all-j-v1.3-groovy.bin")

# def explain_move(move, fen):
#     """
#     Returns natural language explanation for the move in given FEN position
#     """
#     prompt = f"Explain why the move {move} is good in this chess position: {fen}"
#     explanation = model.generate(prompt)
#     return explanation
